PORT=3030
DB_PATH=./data/surfrag.db

# Phase 3.3: Storage toggles (LightRAG is default, vector search is second choice)
# LIGHTRAG_INSERT_ENABLED=true
# LIGHTRAG_URL=http://localhost:9621

# VECTOR_DB_ENABLED=false (default). Set true + API_KEY for vector search.
# VECTOR_DB_ENABLED=false
# API_KEY= 
# EMBED_PROVIDER=glm
# EMBED_MODEL=embedding-2
# VECTOR_DB_PATH=./data/lancedb